# Sample Data (Non-PHI)

These CSV files contain small, synthetic examples to show the export format.

- `plan_attempts_sample.csv`
- `constraint_evaluations_sample.csv`

They are safe to share and do not contain real patient data.
