# Copy to .env and fill in the read-only MongoDB URI provided by the supervisor.
# This project only reads from MongoDB and writes outputs locally.
PLANEVAL_MONGODB_URI="mongodb://user:pass@host:27017/planeval?authSource=admin"
